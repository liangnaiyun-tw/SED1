name: Java auto test

on:
  push:
    branches:
      - auto_test_pr
    paths:
      - '**/midterm/src/**'


jobs:
  build:
    runs-on: [windows-latest]
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '8'
          distribution: 'temurin'
          architecture: x64
      - name: Run the Maven
        run: |
          cd .\midterm\testcase
          .\test.ps1